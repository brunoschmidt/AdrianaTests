

 for (let i = 0; i < array.length; i++) {
     const element = array[i];
     
     let result=null;
     fora: for (let j = 0; j < element.length; j++) {
        const element2 = element[j];
        
        let result=null;
        for (let j = 0; j < element2.length; j++) {
            const element2 = element2[j];
            
            if(condicao == true)
            {
                continue fora;
            }
    
            result = sdljasld;
        }
        console.log(result);
    
        result = sdljasld;
    }
    console.log(result);

}